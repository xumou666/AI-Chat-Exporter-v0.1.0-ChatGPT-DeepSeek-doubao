# 隐私说明 / Privacy Policy

**AI Chat Exporter 不收集、不上传、不存储任何用户数据。**

## 数据流向

- 对话内容只在**你的浏览器本地**被读取（当前标签页的 DOM），并直接在本地生成
  Markdown / JSON / HTML / 纯文本 / ZIP 文件。
- PDF 由浏览器自带的打印功能（"另存为 PDF"）生成。
- 图片导出时，扩展会向你**已经登录的站点自身**发起请求，以下载对话里已经出现过的图片；
  除此之外没有任何网络请求。没有自建服务器、没有统计上报、没有遥测。

## 扩展申请的权限及用途

| 权限 | 用途 |
| --- | --- |
| `activeTab` | 在用户点击工具栏图标时访问当前标签页 |
| `scripting` | 在未预先声明匹配的页面上按需注入面板脚本 |
| `storage` | 保存面板选项与"手动校准"学到的页面结构（`chrome.storage.local`，仅本机） |
| `host_permissions`（chatgpt.com / chat.openai.com / chat.deepseek.com / doubao.com） | 在受支持的对话站点上读取会话 DOM、下载会话中的图片 |

扩展**不申请** `<all_urls>`，也不会读取与对话无关的页面。

## 本地保存的内容

- 面板设置（格式、范围、开关、文件名模板）。
- 手动校准得到的规则（按站点域名存储），用于站点改版后恢复识别。
- 这两类数据都可以随时在扩展管理页删除扩展数据来清除。

## 第三方

- 运行时不包含任何第三方库（ZIP、Markdown 转换、打印文档均为本项目自研）。
- 开发期仅使用 `jsdom` 运行测试，不参与扩展运行。

## 联系方式

如对隐私有疑问，请在本仓库提交 issue。
